



<% @page&.sections&.order(:section_order)&.each do |section| %>
  <% section_type = section.sectionable_type.downcase.gsub('section', '') %>
  <%= render partial: "/section/#{section_type}_section", locals: { section: section.sectionable } %>

  <hr class="bg-citizen-blue-base my-5 mx-4 citizen-blue rounded-full h-8 border-0">
<% end %>

<%= render 'newsfeed' %>



<%= render 'newsletter' %>
<!-- Add your own styling for the login button -->
<style>
  .discord-login-button {
    /* Style your button here */
    background-color: #7289DA; /* Discord's brand color */
    color: white;
    padding: 4px;
    border: none;
    border-radius: 5px;
    text-decoration: none;
    font-size: 16px;
  }
</style>